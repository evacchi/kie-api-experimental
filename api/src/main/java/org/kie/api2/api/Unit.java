package org.kie.api2.api;

/**
 * currently this is nothing more than a "marker" interface
 * although I generally don't love the concept, in this case
 * it serves the purpose of making it easy to distinguish different
 * (sub)types of unit, and to return a type-specific Instance
 * @see UnitInstance
 */
public interface Unit {

}
